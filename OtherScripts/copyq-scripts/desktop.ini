[Commands]
1\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    #appname=\"$(xprop -id $(xprop -root _NET_ACTIVE_WINDOW | cut -d ' ' -f 5) WM_NAME)\"
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #side left
    xdotool windowsize $focused_window_id $screenx $ypos
    xdotool windowmove $focused_window_id  0 $ypos"
1\GlobalShortcut=meta+num+2
1\Icon=\xf15b
1\IsGlobalShortcut=true
1\Name=window bottom full
10\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #center - small
    xdotool windowsize $focused_window_id $xpos $ypos
    xdotool windowmove $focused_window_id $xpos2 $ypos2"
10\GlobalShortcut=meta+num+5
10\Icon=\xf15b
10\IsGlobalShortcut=true
10\Name=window center
2\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    #appname=\"$(xprop -id $(xprop -root _NET_ACTIVE_WINDOW | cut -d ' ' -f 5) WM_NAME)\"
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #side left
    xdotool windowsize $focused_window_id $screenx $ypos
    xdotool windowmove $focused_window_id  0 0"
2\GlobalShortcut=meta+num+8
2\Icon=\xf15b
2\IsGlobalShortcut=true
2\Name=window top full
3\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    #appname=\"$(xprop -id $(xprop -root _NET_ACTIVE_WINDOW | cut -d ' ' -f 5) WM_NAME)\"
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #side left
    xdotool windowsize $focused_window_id $xpos $screeny
    xdotool windowmove $focused_window_id  $xpos 0"
3\GlobalShortcut=meta+num+6
3\Icon=\xf15b
3\IsGlobalShortcut=true
3\Name=window half right
4\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #side left
    xdotool windowsize $focused_window_id $xpos $screeny
    xdotool windowmove $focused_window_id  0 0"
4\GlobalShortcut=meta+num+4
4\Icon=\xf15b
4\IsGlobalShortcut=true
4\Name=window half left
5\Command="
    bash:
    appname=$(xdotool getwindowfocus getwindowname)
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #bottom right
    xdotool windowsize $focused_window_id $xpos $ysize
    xdotool windowmove $focused_window_id $xpos $ypos"
5\GlobalShortcut=meta+num+3
5\Icon=\xf15b
5\IsGlobalShortcut=true
5\Name=window bottom right
6\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    
    focused_window_id=$(xdotool getwindowfocus)
    
    #center - small
    xdotool windowsize $focused_window_id $xpos $ysize
    xdotool windowmove $focused_window_id  0 $ypos"
6\GlobalShortcut=meta+num+1
6\Icon=\xf15b
6\IsGlobalShortcut=true
6\Name=window bottom left
7\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #big
    xdotool windowsize $focused_window_id $screenx $screeny
    xdotool windowmove $focused_window_id 0 0"
7\GlobalShortcut=meta+num++
7\Icon=\xf15b
7\IsGlobalShortcut=true
7\Name=window max
8\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #top left
    xdotool windowsize $focused_window_id $xpos $ysize
    xdotool windowmove $focused_window_id $xpos 0"
8\GlobalShortcut=meta+num+9
8\Icon=\xf15b
8\IsGlobalShortcut=true
8\Name=window top right
9\Command="
    bash:
    #appname=$(xdotool getwindowfocus getwindowname)
    
    test=\"$(xdpyinfo | grep dimensions | sed -r 's/^[^0-9]*([0-9]+x[0-9]+).*$/\\1/')\"
    
    #window=$(xdotool search --onlyvisible --name $appname)
    
    screenx=$(echo $test | cut -d 'x' -f 1)
    screeny=$(echo $test | cut -d 'x' -f 2)
    
    xpos=\"$(expr $screenx / 2 )\"
    ypos=\"$(expr $screeny / 2)\"
    
    xpos2=\"$(expr $xpos / 2)\"
    ypos2=\"$(expr $ypos / 2)\"
    focused_window_id=$(xdotool getwindowfocus)
    
    #top left
    xdotool windowsize $focused_window_id $xpos $ysize
    xdotool windowmove $focused_window_id  0 0"
9\GlobalShortcut=meta+num+7
9\Icon=\xf15b
9\IsGlobalShortcut=true
9\Name=window top left
size=10